
use neon::js::{
    JsArray,
    JsBoolean,
    JsFunction,
    JsNull,
    JsNumber,
    JsObject,
    JsString,
    JsUndefined,
    JsValue,
};
use neon::mem::Handle;

fn set_value_for_styles(
    node: &mut JsObject,
    styles: JsObject,
    getStack: (),
) {
    // FIXME:
}
